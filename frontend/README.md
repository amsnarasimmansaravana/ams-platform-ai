# Frontend (planned)

Per architecture docs:

- **Web:** React 18+, TypeScript, Shadcn/ui, Tailwind, React Flow (workflow canvas)
- **Desktop:** Tauri wrapping the web app

This directory is reserved for the future client. Add `package.json` and app sources here when the UI project starts.
